# 📖 毕设智能管理系统 用户操作手册

> The Academic Curator · 毕业论文全流程协作与 AI 智能评审平台

---

## 目录

- [一、系统登录](#一系统登录)
- [二、学生端操作指南](#二学生端操作指南)
- [三、教师端操作指南](#三教师端操作指南)
- [四、管理员端操作指南](#四管理员端操作指南)
- [五、通用功能说明](#五通用功能说明)
- [六、常见问题](#六常见问题)

---

## 一、系统登录

### 1.1 访问系统

打开浏览器，输入系统地址（如 `http://localhost:5173`），进入登录页面。

### 1.2 选择角色

系统提供三种登录角色，请在登录面板顶部选择：

| 角色 | 图标 | 说明 |
|------|------|------|
| **学生** | 👤 | 上传论文、查看 AI 评审报告、接收教师批注 |
| **教师** | 👤 | 审阅论文、添加批注、设置截止日期、确认定稿 |
| **管理员** | ⚙️ | 管理群组、批量导入、发布公告、系统运维 |

### 1.3 填写登录信息

1. **用户名**：输入学号（学生）/ 工号（教师/管理员）
2. **密码**：输入账户密码
3. **验证码**：点击右侧验证码图片刷新，输入显示的数字（默认 4 位数字）

### 1.4 登录与退出

- 点击「**登录**」按钮进入对应工作台
- 点击侧边栏底部「**退出登录**」安全退出系统
- 忘记密码可点击「**忘记密码**」链接，通过验证码重置密码

---

## 二、学生端操作指南

### 2.1 工作台概览

登录后进入「**学生工作台**」，页面采用三栏布局：

```
┌──────────────────┬───────────────────────────────────┬──────────────────┐
│  左侧边栏         │  主内容区                          │  右侧信息栏       │
│  ├─ 上传论文      │  ├─ 顶部统计卡片（4个指标）         │  ├─ 系统公告      │
│  ├─ 上传附件      │  ├─ 论文分组列表                   │  └─ 截止时间通知  │
│  ├─ 附件列表      │  │   ├─ 指导教师卡片               │                  │
│  ├─ 系统公告      │  │   └─ 论文卡片（含操作按钮）      │                  │
│  └─ 截止时间通知  │  └─ 空状态提示（暂无数据时）        │                  │
└──────────────────┴───────────────────────────────────┴──────────────────┘
```

#### 侧边栏导航

| 按钮 | 功能说明 |
|------|---------|
| **上传论文** | 打开论文上传弹窗，支持选择 .docx/.doc 文件 |
| **上传附件** | 打开附件上传弹窗，支持多种格式文件 |
| **附件列表** | 跳转至附件列表页面，查看所有已上传附件 |
| **系统公告** | 展示最近 3 条系统通知，点击可查看详情 |
| **截止时间通知** | 展示论文 DDL 提醒，红色标记紧急通知 |

#### 顶部统计卡片

工作台顶部显示 4 个核心指标：

| 指标 | 图标 | 说明 |
|------|------|------|
| **待审阅** | 📝 | 已提交但教师尚未审阅的论文数 |
| **已反馈** | ✅ | 教师已给出反馈的论文数 |
| **已定稿** | 📋 | 教师已确认定稿的论文数 |
| **待更新** | 🔄 | 教师要求修改、等待更新的论文数 |

#### 论文分组展示

论文按**指导教师**自动分组展示：

- 每个指导教师为一个卡片组
- 组内显示该教师指导的所有论文
- 论文卡片包含：标题、状态标签、最后更新时间

#### 论文状态标签

| 状态 | 颜色 | 说明 |
|------|------|------|
| **待审阅** | 蓝色 | 已上传，等待教师审阅 |
| **审阅中** | 橙色 | 教师正在审阅 |
| **待修改** | 红色 | 教师已反馈，需修改 |
| **已更新** | 紫色 | 已提交新版本，等待再次审阅 |
| **已定稿** | 绿色 | 教师确认最终定稿 |

---

### 2.2 上传论文

#### 首次上传流程

1. 在侧边栏点击「**上传论文**」按钮，打开上传弹窗
2. 弹窗显示以下信息：
   - 论文标题输入框（可自定义标题）
   - 选择指导教师（下拉选择或搜索）
   - 文件选择区域
3. 点击选择文件，弹出文件选择器
4. 选择 Word 文档（**支持 .docx / .doc 格式**）
5. 可选勾选「**是否为最终版本**」
6. 点击「**确认上传**」

#### 上传后自动流程

- 上传成功后，系统**自动触发 AI 智能评审**
- AI 评审任务状态显示为「**评审中**」
- 评审完成后（约 30 秒 ~ 2 分钟），状态变为「**待审阅**」
- 学生可在论文卡片上查看 AI 评审报告

#### 上传限制

- 仅支持 **.docx / .doc** 格式
- 上传前请确保文件未损坏
- 如已有论文，系统将提示进入**版本更新**流程

---

### 2.3 版本更新

当论文状态为「**待修改**」时，可进行版本更新：

#### 操作步骤

1. 在论文卡片上找到「**更新论文**」按钮（仅在状态允许时显示）
2. 点击后打开版本更新弹窗
3. 选择修改后的新版本文件
4. 确认更新

#### 版本管理规则

- 每次更新生成新版本，保留完整历史记录
- 版本号自动递增（如 v1.0 → v2.0）
- 支持**版本对比**功能，可查看历次修改差异
- 更新后状态变为「**已更新**」，等待教师再次审阅
- **定稿后不可再更新**

#### 更新按钮不可用的情况

| 状态 | 提示信息 |
|------|---------|
| 已定稿 | "论文已定稿，无法再进行更新" |
| 已提交更新 | "论文已提交更新，请等待教师审阅" |
| 审阅中 | "论文正在等待教师审阅，请耐心等待" |
| 待审阅 | "论文刚上传，请等待教师开始审阅" |

---

### 2.4 查看 AI 评审报告

上传论文后，系统会自动触发 AI 智能评审：

#### 查看条件

- 仅当 AI 评审任务完成后才可查看
- 状态为「**评审中**」时按钮不可用（灰色）
- 评审完成后，「**查看评审**」按钮变为可用

#### 报告内容

点击「**查看评审**」按钮，进入评审报告页面：

- **结构分析**：论文结构完整性评估
- **内容质量**：论点、论据、创新性评分
- **格式规范**：引用格式、排版规范检查
- **修改建议**：具体的改进方向和建议
- **风险等级**：高 / 中 / 低风险标记

> ⏱️ 评审时间：上传后约 30 秒 ~ 2 分钟，请耐心等待。

---

### 2.5 查看教师批注与审阅意见

教师审阅后会在论文中添加批注和整体评价：

#### 查看条件

- 论文状态为「**待修改**」或「**已定稿**」时可查看
- 状态为「**待审阅**」时不可查看（提示"论文处于待审阅阶段，暂不可查看"）

#### 操作步骤

1. 在论文卡片上点击「**查看批注**」按钮
2. 进入论文预览 + 批注列表页面
3. 页面左侧显示论文内容，右侧显示批注列表

#### 批注列表内容

每条批注包含：
- **选中内容**：教师批注对应的原文段落
- **批注内容**：教师的修改建议
- **建议内容**：具体的改进方向
- **来源**：教师批注 / AI 批注
- **时间**：批注添加时间

#### 批注定位

- 点击批注列表中的条目，自动滚动到论文对应位置
- 批注在正文中以高亮或下划线标记
- 支持富文本显示（加粗、列表等格式）

---

### 2.6 论文预览

点击论文卡片标题或「**预览**」按钮，进入论文预览页面：

#### 预览方式

- **Word 文档**：使用 docx-preview 组件在线渲染
- **PDF 文档**：使用 pdfjs-dist 组件渲染
- 支持缩放（100% ~ 200%）
- 支持翻页导航

#### 批注列表面板

- 右侧常驻显示**批注列表面板**（如有批注）
- 显示批注数量统计
- 支持按来源筛选（全部 / 教师 / AI）
- 空状态提示"暂无批注"

---

### 2.7 下载论文

1. 在论文卡片上点击「**下载论文**」按钮
2. 选择下载格式（.docx）
3. 文件将保存到本地设备

---

### 2.8 附件管理

#### 上传附件

1. 在侧边栏点击「**上传附件**」按钮
2. 打开附件上传弹窗
3. 选择要上传的附件文件
4. 支持格式：
   - 文档：.docx, .doc, .pdf, .txt
   - 表格：.xlsx
   - 演示：.pptx
   - 压缩：.zip, .rar
5. 确认上传

#### 查看附件列表

1. 在侧边栏点击「**附件列表**」
2. 进入附件列表页面
3. 显示所有已上传附件的：文件名、大小、上传时间
4. 支持预览和下载操作

#### 附件统计

工作台侧边栏显示附件统计：
- 文档类数量
- 表格类数量
- 其他类型数量
- 附件总数

---

### 2.9 通知与公告

#### 系统公告

- 侧边栏展示最近 3 条系统公告
- 点击公告条目查看详情
- 点击「**查看更多**」跳转公告详情页
- 公告包含：标题、内容、发布时间、发布人

#### 截止时间通知

- 侧边栏展示论文 DDL 提醒
- 紧急通知以**红色标识**显示
- 点击可查看具体的截止时间要求

#### 消息类型

| 类型 | 图标 | 说明 |
|------|------|------|
| 系统通知 | 📢 | 管理员发布的公告 |
| 教师反馈 | 📝 | 教师的审阅意见 |
| DDL 提醒 | ⏰ | 论文截止日期提醒 |
| 版本更新 | 🔄 | 论文版本状态变更 |

---

### 2.10 我的论文页面

点击导航可进入「**我的论文**」独立页面：

- 以列表形式展示所有论文
- 支持按状态筛选
- 显示论文标题、版本号、指导教师、更新时间
- 支持论文的预览、下载、更新操作

---

### 2.11 刷新与数据同步

#### 自动刷新

- 页面进入时自动加载最新数据
- 上传/更新后自动刷新论文列表
- 状态轮询机制确保数据实时性

#### 手动刷新

- 下拉页面触发刷新
- 刷新后更新统计卡片和论文列表
- 显示刷新结果提示（"已更新 N 篇论文状态"）

---

## 三、教师端操作指南

### 3.1 教师仪表盘概览

登录后进入「**教师仪表盘**」，页面采用动态布局：

```
┌──────────────────┬───────────────────────────────────┐
│  侧边栏           │  主内容区                          │
│  ├─ 选择班级      │  ├─ 班级选择视图（初始页面）        │
│  ├─ 全部论文      │  ├─ 论文列表视图（选择班级后）       │
│  ├─ 待审阅        │  ├─ 统计概览卡片                   │
│  ├─ 待更新        │  └─ 学生论文卡片 + 操作按钮         │
│  ├─ 消息中心      │                                    │
│  └─ 截止日期      │                                    │
└──────────────────┴───────────────────────────────────┘
```

#### 侧边栏导航

| 按钮 | 图标 | 说明 |
|------|------|------|
| **选择班级** | dashboard_customize | 返回班级选择页面 |
| **全部论文** | article | 显示该班级所有论文 |
| **待审阅** | pending_actions | 筛选待审阅的论文 |
| **待更新** | update | 筛选等待学生修改的论文 |
| **消息中心** | mail | 查看系统消息（未读显示角标） |
| **截止日期** | event | 打开截止日期设置弹窗 |

#### 顶部标题栏

- 左侧：系统名称「**论文管理系统**」
- 右侧：
  - 通知图标（点击跳转消息中心）
  - 个人头像（点击跳转个人中心）

---

### 3.2 选择班级

首次进入或点击「**选择班级**」时，显示班级选择视图：

#### 班级卡片信息

每个班级卡片显示：
- **班级名称**：如「22 智能 1 班」
- **班级编号**：如 `202201`
- **统计数字**：
  - 待审阅数（红色）
  - 待更新数（橙色）

#### 操作步骤

1. 浏览班级卡片列表
2. 点击目标班级卡片
3. 进入该班级的论文管理页面

#### 空状态

- 暂无班级数据时显示空状态图标和提示文字
- 可检查网络连接或联系管理员

---

### 3.3 论文列表与筛选

进入班级后，主内容区显示该班级所有学生的论文：

#### 页面标题区

显示：「**学生论文库**」+ 当前待审阅数量高亮提示

```
欢迎回来，当前有 [N] 篇论文等待您的审阅
```

#### 筛选方式（侧边栏）

| 筛选项 | 显示内容 | 图标 |
|--------|---------|------|
| **全部论文** | 该班级所有学生的论文 | article |
| **待审阅** | 学生已提交、教师尚未审阅 | pending_actions |
| **待更新** | 教师已反馈、等待学生修改 | update |

#### 学生论文卡片

每张卡片包含：
- **学生信息**：姓名、学号
- **论文信息**：
  - 论文标题（如「未命名论文」则显示默认标题）
  - 当前版本号
  - 最后更新时间
- **状态标签**：
  - 待审阅 / 已反馈 / 已更新 / 已定稿 / 未上传
- **操作按钮**：
  - 有论文时：「**审阅**」按钮
  - 无论文时：显示「**暂未上传论文**」

#### 分页

- 论文列表支持分页加载
- 底部显示页码导航
- 支持上一页 / 下一页切换

---

### 3.4 论文审阅

#### 进入审阅

1. 在论文列表中点击「**审阅**」按钮
2. 打开论文审阅弹窗（全屏遮罩）
3. 弹窗布局：

```
┌─────────────────────────────────────────────────────────┐
│  顶部工具栏                                              │
│  ├─ 论文标题 + 学生姓名                                  │
│  ├─ 字数统计                                             │
│  └─ 操作按钮：添加批注 | 提交审阅意见 | 确认定稿         │
├─────────────────────────────────────────────────────────┤
│  左侧：论文预览区（Word/PDF 渲染）                       │
│  ├─ 支持缩放（100% ~ 200%）                             │
│  ├─ 支持翻页                                            │
│  └─ 选中文本高亮                                        │
├─────────────────────────────────────────────────────────┤
│  右侧：批注与审阅面板                                    │
│  ├─ AI 评审摘要（可折叠）                                │
│  ├─ 批注列表（按时间倒序）                               │
│  ├─ 审阅意见输入框                                      │
│  └─ 提交审阅意见按钮                                     │
└─────────────────────────────────────────────────────────┘
```

---

#### 3.4.1 添加批注

**操作步骤**：

1. 在论文预览区域**用鼠标选中一段文字**
2. 点击顶部「**添加批注**」按钮
3. 如未选中文本，弹出提示「请先选中文本再添加批注」
4. 在弹窗中输入批注内容（支持多行文本，最多 2000 字）
5. 点击「**保存**」

**批注内容格式**：
```
选中内容：[原文段落]
批注：[教师的具体修改建议]
建议：[改进方向]
```

**批注管理**：
- 右侧批注列表显示所有已添加批注
- 点击批注可定位到论文对应位置（自动滚动高亮）
- 支持**编辑**已添加的批注
- 支持**删除**批注（需二次确认）
- 批注按时间倒序排列

---

#### 3.4.2 提交审阅意见

**操作步骤**：

1. 点击顶部「**提交审阅意见**」按钮
2. 在弹窗中输入整体评价（最多 2000 字）
3. 支持多行文本输入
4. 点击「**提交**」
5. 已提交过审阅意见时，按钮变为「**修改审阅意见**」

**审阅意见效果**：
- 学生将在工作台收到通知
- 论文状态变为「**待修改**」
- 学生可查看审阅意见并更新论文

---

#### 3.4.3 确认定稿

当论文质量达标、无需再修改时：

**操作步骤**：

1. 在审阅页面点击「**确认定稿**」按钮
2. 弹出确认对话框：
   ```
   此操作将确认论文最终定稿，不可撤销。
   是否确认？
   ```
3. 点击「**确认**」
4. 论文状态变为「**已定稿**」

**定稿后效果**：
- 学生无法继续上传新版本
- 学生可查看所有批注和审阅意见
- 论文进入最终归档状态

> ⚠️ **注意**：定稿后学生无法继续上传新版本，请确保论文质量合格后再执行此操作。

---

#### 3.4.4 AI 评审摘要

审阅页面右侧显示 AI 评审摘要面板：

- **可折叠**：点击标题可展开/收起
- **内容**：AI 自动生成的论文结构分析、内容质量评分
- **参考**：教师可结合 AI 摘要给出更精准的批注

---

#### 3.4.5 论文预览操作

- **缩放**：使用缩放按钮调整预览比例（100% ~ 200%）
- **翻页**：支持上一页 / 下一页导航
- **选中文本**：鼠标拖动选中文本，系统记录选中范围
- **定位批注**：点击批注列表中的条目，自动滚动到对应位置

---

### 3.5 消息中心

#### 进入方式

- 侧边栏点击「**消息中心**」
- 或点击顶部通知图标

#### 消息列表

显示所有接收到的消息：

| 信息 | 说明 |
|------|------|
| 消息标题 | 消息的简要标题 |
| 消息内容 | 详细内容摘要 |
| 来源 | 发送方（系统/管理员/学生） |
| 时间 | 发送时间 |
| 已读状态 | 未读消息显示红点标记 |

#### 操作功能

- **查看详情**：点击消息卡片打开详情弹窗
- **标记已读**：单条标记或「**全部已读**」
- **消息类型**：
  - 系统通知
  - 学生提交提醒
  - 版本更新提醒
  - DDL 提醒

#### 空状态

- 暂无消息时显示「📭 暂无消息」
- 加载失败时显示红色提示「加载失败，请刷新后重试」

---

### 3.6 截止日期管理

#### 打开设置

1. 在侧边栏点击「**截止日期**」
2. 打开「**论文提交截止设置**」弹窗

#### 设置步骤

1. **选择日期**：
   - 使用日期选择器（年-月-日）
   - 默认显示当前日期
   - 不可选择过去日期
2. **查看预览**：
   - 显示完整的截止时间预览
   - 格式：`YYYY-MM-DD`
3. **保存设置**：
   - 点击「**保存设置**」按钮
   - 显示「保存中...」状态
   - 保存成功后弹窗关闭

#### 删除截止日期

- 已设置 DDL 的群组可点击「**删除**」按钮
- 弹出确认对话框：「确定要删除当前的截止日期设置吗？」
- 点击「**确定**」移除 DDL

#### 全部班级汇总视图

- 留空查询条件 → 显示「全部班级截止日期汇总」
- 该视图为**只读模式**
- 展示所有班级 DDL 总览
- 未设置的班级显示「**未设置**」
- 支持深色/浅色模式切换

---

### 3.7 论文下载

#### 单篇下载

1. 在论文卡片上点击「**下载**」按钮
2. 打开下载弹窗
3. 显示论文信息（学生姓名、论文标题）
4. 自动打包为 ZIP 压缩包
5. 点击「**确认下载**」下载到本地

#### 批量下载

1. 在论文列表中勾选多篇论文（使用复选框）
2. 点击「**批量下载**」按钮
3. 打开批量下载弹窗
4. 显示已选论文列表
5. 点击「**确认下载**」
6. 系统将所有选中论文**打包为 ZIP 压缩包**
7. 下载压缩包到本地

> 📦 下载内容包括该群组下所有学生的论文文件。

---

### 3.8 个人中心

点击顶部头像进入「**个人中心**」：

#### 用户信息卡片

- **头像**：显示姓名首字母
- **姓名**：教师姓名
- **工号**：教师工号
- **角色**：教师

#### 统计信息

| 统计项 | 图标 | 说明 |
|--------|------|------|
| 指导学生 | 👥 | 当前指导学生总数 |
| 待审阅 | 📝 | 待审阅论文数 |
| 已反馈 | ✅ | 已给出反馈的论文数 |
| 已定稿 | 📋 | 已确认定稿的论文数 |

#### 功能菜单

| 菜单项 | 功能 |
|--------|------|
| **修改密码** | 修改登录密码（需输入原密码 + 新密码 + 确认密码） |
| **关于系统** | 查看系统版本信息（毕业论文管理系统 v1.0） |

#### 退出登录

- 点击「**退出登录**」按钮
- 弹出确认对话框
- 确认后清除登录状态，返回登录页

---

### 3.9 统计与数据刷新

#### 班级统计

班级卡片实时显示：
- 学生总数
- 待审阅数
- 待更新数
- 已定稿数

#### 论文状态统计

论文列表顶部显示：
- 当前筛选条件下的论文总数
- 待审阅数量高亮

#### 数据刷新

- 页面进入时自动加载最新数据
- 切换班级时重新加载
- 审阅操作后自动刷新状态

---

## 四、管理员端操作指南

### 4.1 系统管理概览

登录后进入「**系统管理**」页面，左侧为导航栏，右侧为主内容区：

| 导航项 | 功能 |
|--------|------|
| **群组管理** | 创建/编辑/删除群组，批量导入师生关系，论文打包下载 |
| **用户与权限** | 管理用户账户、批量导入用户、分配权限 |
| **公告中心** | 撰写和发布系统公告 |
| **控制台** | 系统运维、查看截止日期管理、查看群组关系 |
| **查看群组关系** | 以表格形式查看所有群组的师生关系 |

---

### 4.2 群组管理

#### 4.2.1 创建群组

1. 在「群组管理」页面点击右上角「**+ 创建群组**」按钮
2. 在弹窗中填写：
   - **群组名称**（必填）：如「22 智能 1 班」
   - **群组编号**（选填）：纯数字，如 `202201`；留空则由系统自动生成
3. 点击「**保存**」

> 💡 **提示**：群组编号一旦创建不可修改，请谨慎填写。

#### 4.2.2 编辑群组

1. 在群组列表中找到目标群组
2. 点击「**编辑**」按钮
3. 修改群组名称等信息
4. 点击「**保存**」

#### 4.2.3 删除群组

1. 在群组列表中找到目标群组
2. 点击「**删除**」按钮
3. 弹出确认对话框，提示「此操作不可撤销」
4. 点击「**确认删除**」

> ⚠️ **警告**：删除群组将清空该群组下所有师生关系数据，请谨慎操作。

#### 4.2.4 查询群组

1. 在搜索框中输入**教师工号**或**群组名称**
2. 点击「**查询群组**」按钮
3. 留空可加载全部群组

---

### 4.3 批量导入师生关系

#### 4.3.1 下载模板

1. 在「群组管理」页面找到「**批量导入师生关系**」区域
2. 点击「**下载模板文件**」按钮
3. 下载 `师生关系导入模板.xlsx`

#### 4.3.2 填写模板

模板必须包含以下列：

| 列名 | 必填 | 格式说明 |
|------|------|---------|
| 群组编号 | 是 | 纯数字，与已有群组匹配 |
| 群组名称 | 是 | 如「22 智能 1 班」 |
| 教师工号 | 是 | 系统中已存在的教师工号 |
| 教师姓名 | 是 | 与工号对应的教师姓名 |
| 学生学号 | 是 | 系统中已存在的学生学号 |
| 学生姓名 | 是 | 与学号对应的学生姓名 |

> 📋 **导入说明**：
> - 支持 **CSV（.csv）** 和 **XLSX（.xlsx）** 格式
> - 文件大小不超过 **10MB**
> - 群组编号为纯数字

#### 4.3.3 上传文件

1. 点击「**选择文件上传**」按钮
2. 选择填写好的模板文件
3. 文件信息显示在按钮下方（含文件名、大小）
4. 点击「**开始导入**」按钮

#### 4.3.4 前置验证与缺失账号处理

上传后系统自动进行**前置验证**：

**情况一：无缺失账号**
- 直接导入成功
- 显示导入结果（成功条数、失败条数）

**情况二：存在缺失账号**
- 弹出「**师生信息验证结果**」弹窗
- 显示缺失的教师工号和学生学号列表
- 询问「**是否继续导入并自动创建缺失账号？**」

**操作选项**：
- 点击「**继续导入**」→ 系统自动创建缺失账号并完成导入
- 点击「**取消**」→ 中断导入，返回修改模板

---

### 4.4 论文打包下载

#### 4.4.1 查询群组

1. 在「群组管理」页面找到「**论文打包下载**」区域
2. 在「**群组名称**」输入框中输入群组编号或名称
3. 点击「**更新预计**」按钮查询

#### 4.4.2 查看预计下载量

- 系统显示「**预计下载：N 个文件**」
- 确认下载范围正确

#### 4.4.3 开始下载

1. 勾选「**打包为 ZIP 文件**」选项（默认勾选）
2. 点击「**开始下载**」按钮
3. 系统生成 ZIP 压缩包并自动下载

> 📦 下载内容包括该群组下所有学生的论文文件。

---

### 4.5 用户与权限

#### 4.5.1 批量导入用户

1. 点击左侧「**用户与权限**」导航
2. 在「用户批量导入」区域点击「**下载模板文件**」
3. 下载 `用户导入模板.xlsx`（模板 ID: `tpl_20a279a0`）
4. 填写后点击「**选择文件上传**」→「**开始导入用户**」

#### 4.5.2 查看导入结果

- 导入结果显示在下方「**导入结果**」区域
- 显示新建用户数、更新用户数、错误明细

#### 4.5.3 管理用户

- 查看用户列表
- 编辑用户信息
- 删除用户账户（二次确认）

---

### 4.6 公告中心

#### 4.6.1 撰写公告

1. 点击左侧「**公告中心**」导航
2. 点击「**撰写新公告**」卡片
3. 在弹窗中填写：
   - **标题**
   - **正文内容**
4. 点击「**保存**」

#### 4.6.2 发布公告

- 保存后公告自动发布到系统
- 学生和教师将在工作台收到通知

---

### 4.7 控制台

控制台提供系统运维功能：

#### 4.7.1 截止日期管理

1. 在控制台中点击「**查看与管理截止日期**」
2. 按群组编号查询各群组的 DDL 设置
3. 可查看、修改或删除截止日期

#### 4.7.2 查看群组关系

1. 点击左侧「**查看群组关系**」导航
2. 以表格形式查看所有群组的师生关系
3. 表格列：群组名称、群组编号、创建时间、姓名、工号/学号

---

## 五、通用功能说明

### 5.1 个人中心

- 点击右上角头像 → 进入「**个人中心**」
- 可查看和修改个人信息
- 查看系统版本信息（毕业论文管理系统 v1.0）

### 5.2 深色模式

部分页面支持深色/浅色模式切换（如教师端截止日期设置）

### 5.3 文件格式支持

| 类型 | 支持格式 |
|------|---------|
| 论文上传 | .docx, .doc |
| 附件上传 | .docx, .doc, .pdf, .xlsx, .pptx, .txt, .zip, .rar |
| 论文下载 | .docx, .pdf |
| 批量下载 | ZIP 压缩包 |

### 5.4 跨端使用

本系统支持多平台访问：
- **H5 网页**：通过浏览器访问
- **微信小程序**：搜索小程序名称
- **App**：下载安装 Android/iOS 客户端

---

## 六、常见问题

### Q1：为什么 AI 评审按钮显示为灰色？

AI 评审需要在上传论文后等待系统处理完成。上传后请等待 30 秒 ~ 2 分钟，刷新页面后按钮将变为可用状态。

### Q2：上传论文后显示「暂无论文数据」？

首次上传后，请刷新工作台页面。如仍不显示，请检查网络连接并重新登录。

### Q3：批量导入师生关系时提示「部分教师工号或学生学号在数据库中不存在」？

这表示模板中填写的工号/学号尚未在系统中注册。您可以选择：
- **继续导入**：系统自动创建缺失账号
- **取消**：先在「用户与权限」中导入用户，再导入师生关系

### Q4：教师确认定稿后还能修改吗？

不能。定稿为最终状态，学生无法继续上传新版本。如需修改，请联系管理员重置论文状态。

### Q5：论文打包下载为什么显示「预计下载：0 个文件」？

请先输入群组名称或编号，点击「**更新预计**」按钮查询。留空或输入不存在的关键词将导致查询结果为空。

### Q6：验证码看不清怎么办？

点击验证码图片即可刷新，获取新的验证码。

### Q7：忘记密码怎么办？

在登录页面点击「**忘记密码**」链接，通过绑定的手机号或邮箱接收验证码进行重置。

---

> 📅 手册版本：v1.0  
> 📝 更新日期：2026-04-22  
> 📂 文件位置：`/USER_MANUAL.md`
